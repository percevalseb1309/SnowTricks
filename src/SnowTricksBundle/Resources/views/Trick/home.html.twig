{% extends "@SnowTricks/layout.html.twig" %}

{% block title %}
  Home - {{ parent() }}
{% endblock %}

{% block snowtricks_body %}
	<h1>Home page</h1>

	{% for row in listTricks|batch(4) %}
		<div class="row">
		{% for trick in row %}
			<div class="col-md-3">
			  	<div class="thumbnail">
			    	<a href="{{ path('trick_show', {'slug': trick.slug}) }}">
			    		{% if trick.pictures is not empty %}
				      		<img src="{{ asset(trick.pictures|first.webPath) }}" alt="{{ trick.pictures|first.alt }}" style="width:100%">
				      	{% else %}
				      		<img src="{{ asset('uploads/pictures/default.jpeg') }}" alt="Image par défaut" style="width:100%">
			    		{% endif %}
			    	</a>
			      	<div class="caption">
			        	<a href="{{ path('trick_show', {'slug': trick.slug}) }}">{{ trick.name }}</a>
			        	<div class="pull-right">
				        	<a href="{{ path('trick_edit', {'slug': trick.slug}) }}"><span class="glyphicon glyphicon-edit"></span></a>
				        	<a href="{{ path('trick_delete', {'slug': trick.slug}) }}"><span class="glyphicon glyphicon-trash"></span></a>
			        	</div>
			      	</div>
			  	</div>
			</div>
		{% endfor %}
		</div>
	{% endfor %}
{% endblock %}