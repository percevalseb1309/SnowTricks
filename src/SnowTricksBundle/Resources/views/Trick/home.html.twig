{% extends "@SnowTricks/layout.html.twig" %}

{% block title %}
  Home - {{ parent() }}
{% endblock %}

{% block snowtricks_body %}
	<h1>Home page</h1>

	{% for message in app.session.flashbag.get('notice') %}
	    <div class="alert alert-success">Message flash : {{ message }}</div>
	{% endfor %}

	{% for row in listTricks|batch(4) %}
		<div class="row">
		{% for trick in row %}
			<div class="col-md-3">
			  	<div class="thumbnail">
			    	<a href="{{ path('snow_tricks_details', {'slug': trick.slug}) }}">
				      	<img src="" alt="" style="width:100%">
			    	</a>
			      	<div class="caption">
			        	<p><a href="{{ path('snow_tricks_details', {'slug': trick.slug}) }}">{{ trick.name }}</a></p>
			      	</div>
			  	</div>
			</div>
		{% endfor %}
		</div>
	{% endfor %}
{% endblock %}